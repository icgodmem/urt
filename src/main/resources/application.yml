spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:postgresql://localhost:5432/urt
    username: postgres
    password: gomito21
    hikari:
      poolName: Hikari
      auto-commit: false
      
  jpa:
    hibernate:
      ddl-auto: update